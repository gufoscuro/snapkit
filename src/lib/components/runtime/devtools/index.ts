// Public API exports
export { registerForm, registerPageDetails, registerPageState } from './devtools-registry.svelte'
export type { FormDebugInfo, PageDetailsInfo, FormDataGetter } from './types'

// Note: DevtoolsPanel is rendered in root layout, not imported directly
