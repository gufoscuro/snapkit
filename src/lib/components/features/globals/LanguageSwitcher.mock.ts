/**
 * Mock data for LanguageSwitcher component preview
 *
 * This file provides realistic example data for the admin panel block preview system.
 * LanguageSwitcher has no props - it uses context internally.
 * This mock file exists for consistency in the component system.
 */

/**
 * LanguageSwitcher doesn't accept props - it uses LanguageContext internally.
 * The component is self-contained and manages its own state.
 */
export const mockLanguageSwitcher = {}

/**
 * Default export for consistency
 */
export default mockLanguageSwitcher
