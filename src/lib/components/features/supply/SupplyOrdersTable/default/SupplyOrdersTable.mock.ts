/**
 * Mock data for SupplyOrdersTable component preview
 *
 * This file provides realistic example data for the admin panel block preview system.
 * When creating mock data, always use plausible, business-relevant values.
 */

/**
 * SupplyOrdersTable doesn't accept props - it's a self-contained component
 * that manages its own data fetching, state, and rendering.
 *
 * The component uses:
 * - apiRequest to fetch data from 'supply/order' endpoint
 * - Internal state for data, loading, loadingMore, hasMore
 * - DataTable for rendering with defined columns
 *
 * This mock file exists for consistency in the component system.
 */
export const mockSupplyOrdersTable = {}

/**
 * Default export for consistency
 */
export default mockSupplyOrdersTable
