/**
 * @deprecated Use tenantConfigStore directly
 * Kept for backward compatibility during migration
 */
export type NavItem = {
  label: string
  href: string
  visible?: boolean
  disabled?: boolean
}
