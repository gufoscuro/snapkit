# API gateway base URL — read via $env/dynamic/public (must be PUBLIC_-prefixed).
# Do NOT include the trailing /api; request.ts appends it.
PUBLIC_API_GATEWAY=http://arke.localhost.me:5175

# Gemini (Google AI Studio) — used by /api/chat/gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
